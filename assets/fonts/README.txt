Site typography uses **Inter** from Google Fonts (see `/assets/site.css`).

This folder is only for optional self-hosted `.woff2` files if you add
custom fonts later; nothing is required here for the current design.
